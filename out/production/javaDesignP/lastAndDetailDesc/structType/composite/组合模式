组合模式

    场景是：
        比如遇到整体与部分有包含关系，可以用树形结构表示的时候

    定义是：
        将有关系的对象组合成树形结构以表示“整体-部分”的关系。使得用户对单个对象和组合对象的使用保持一致

    本质是：
        统一叶子对象和组合对象。也就是统一各个节点的对象

    实现是：
        为了统一各个节点的对象使用，他们需要有同一个基类；
        区别在于，一般的，节点子类会维护一个List来作为下一个子节点的容器；而叶子节点不需要这个List

