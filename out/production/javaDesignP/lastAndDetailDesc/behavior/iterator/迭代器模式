迭代器模式

    场景是
        要以统一的方式访问不同的聚合对象内部元素
        再简单的说法就是以统一的方式访问不同的集合

    定义是
        提供一种方法，顺序访问聚合对象中的各个元素，而又不需要暴露该对象的内部表示

    本质是
        控制访问聚合对象中的元素

    实现是

        既然是通过迭代器顺序访问聚合对象内元素的，那么迭代器持有聚合对象实例
        迭代器有自己的接口，定义访问方式，比如第一个，下一个等

        迭代器单独存在是毫无意义的，迭代器要与聚合对象建立关系：
        迭代器持有聚合对象实例，聚合对象提供方法返回关联的迭代器对象，这样才能方便通过聚合对象获取它的迭代器
        具体聚合对象内部其实是维护了一个元素数组的，提供获取size和item的方法

        （代码中发现迭代器主要操作的是索引，所以关系应该是  聚合对象--索引--迭代器）


