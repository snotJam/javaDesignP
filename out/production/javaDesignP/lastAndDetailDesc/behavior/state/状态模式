状态模式：
    场景是：
        简单的来说，就是要应对多种不同的状态来做不同的处理

    定义是：
        允许对象在其内部状态改变的时候，改变他的行为。

    本质是：
        根据状态来分离和选择行为

    实现是：
        按照定义的说法，状态改变的时候改变行为，那么状态可以和行为绑定
        也就是说要执行的操作是和状态绑定的，是状态类内部的
        为对象设置状态，在其方法内部调用状态的方法

