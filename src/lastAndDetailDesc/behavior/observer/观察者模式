观察者模式：

    场景是：
        当一个对象的状态发生变化的时候，要通知其所有依赖者。比如报社发放报纸，要通知客户有新的报纸发放

    定义是：
        定义对象间一对多的依赖关系，当一个对象发生变化的时候，所有依赖于它的对象都能得到通知，并被自动更新

    本质是：
        触发联动

    实现是：
        被观察者（目标对象）要提供注册和注销功能，以方便维护所有依赖的观察者
        因为要触发联动，当目标对象改变的时候触发观察者的某个方法，所有观察者可以统一实现一个接口，声明联动时的方法

