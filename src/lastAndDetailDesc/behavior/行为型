行为型设计模式，感觉一般是解决操作顺序之类的

责任链模式：
    需要将一类对象进行多次，不同逻辑的处理的时候


命令模式：
    调用者集成各种命令，命令持有被调用对象。


迭代器模式：
    持有聚合对象，然后对其操作


中介者模式：
    多个对象之间相互关联，相互影响
    对象持有中介者，变化的时候通知中介者，中介者内部调用其他对象变化


观察者模式：
    1对多，需要回调的时候。



状态模式：
    多种状态，对应不同操作的时候
    将状态与操作绑定，与对象分离。对象可以设置状态


策略模式：
    与状态相似。
    但是状态是对象自己的状态，是属性，分离
    策略不是对象的，是外部的


模板方法模式：
    子类操作都一样，实现不同。
    属于我们最常用的感觉














