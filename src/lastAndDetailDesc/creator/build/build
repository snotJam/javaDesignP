build：
    场景是：同样的构建过程，得到不一样的对象

    定义是：将一个复杂对象的构建与其表示分离，使得同样的过程可以构建不同的对象

    本质是：分离整体构建算法与部件构造,以达到同样的构建过程传递不同的参数可以创建出不同的对象

    实现是：在目标对象类中定义静态内部类Builder，持有和目标对象类一样的成员属性
            1：目标对象类的构造方法包含内部静态类对象build作为参数
            2：Builder类中提供build方法创建目标类对象，set属性的方法返回Builder自己的对象